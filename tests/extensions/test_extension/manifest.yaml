name: TestExtension
authors:
  - Jens Widell <jens@critic-review.org>
description: Test extension

package:
  type: python
  entrypoints:
    handle_endpoint:
      target: test_extension:handle_endpoint

roles:
  - type: endpoint
    description: Endpoint for doing stuff.
    name: main
    entrypoint: handle_endpoint
